import React from 'react';
import ReactDOM from 'react-dom/client';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <div>
    <h1>🎉 기억정원 프론트엔드 컨테이너 구동 성공!</h1>
    <p>이제 코드를 수정하면 화면에 바로 반영됩니다.</p>
  </div>
);